syntax = "proto3";

package suggest_request;

option go_package = "suggest/suggest_request";

message Request {
  string part = 1;
  repeated string class = 3;
  repeated string exclude_class = 4;
  int64 page = 5;
  int64 count = 6;
  enum Display {
    Escaped = 0;
    Unescaped = 1;
  }
}